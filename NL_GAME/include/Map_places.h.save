#ifndef MAP_PLACES_H
#define MAP_PLACES_H
#include<string>

class Map_places
{
    public:
        Map_places(sf::IntRect bounds,std::int id,);
        sf::IntRect getIntRect();
        void setIntRect(sf::IntRect rect);
        int getId();
        std::string getName();
    protected:
    private:
        sf::IntRect bounds;
        std::int id;
        std::string place_name;
};

#endif // MAP_PLACES_H
